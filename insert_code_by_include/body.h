std::cout << "Congratulation! Now we are at function_from_h body, it is writen in separate file and included by preprocessor dirrectory \n";

std::cout << "2*arg1 = " << 2 * arg1 << std::endl; // comment in body.h

return 2 * arg1;
